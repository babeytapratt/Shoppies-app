import React from 'react';
import '../../scssFiles/headerContainer.scss'
import Logo from '../../ui/Logo/Logo';


const HeaderContainer = () => {
    return (
        <header>
            <Logo />
        </header>
    )
}

export default HeaderContainer;
